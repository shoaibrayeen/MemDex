---
memdex:
  id: mem_9a7f8028
  title: Discovery is never a repository sweep
  category: conventions
  description: Only configured locations are read.
  importance: 0.6
  source: MEMORY.md > Conventions > Discovery is never a repository sweep
---
# Discovery is never a repository sweep

Only configured locations are read. A README or a changelog is documentation, not
assistant memory, and silently rewriting one would be the worst possible
surprise.
